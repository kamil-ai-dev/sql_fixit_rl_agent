---
title: SQL Debug Env
emoji: 🐛
colorFrom: blue
colorTo: indigo
sdk: docker
app_port: 8000
pinned: false
---

# SQL Debug Environment

A SQL debugging environment built with FastAPI and Docker.

## Usage

This space runs a FastAPI server for SQL debugging. Access the API at `/docs` for interactive documentation.

## Configuration

See the `Dockerfile` for build configuration.
